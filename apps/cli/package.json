{
  "name": "@memoryos/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": { "memoryos": "dist/entry.js" },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/entry.js",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
